# Agent entrypoint — video-art

**Open this next, and only this as the map:**

[`docs/video-os/00-INDEX.md`](docs/video-os/00-INDEX.md)

Then `docs/video-os/04-QUALITY-CONTRACT.md`. Then **one** job file (01 create · 02 rebuild · 03 reel log).

This file is a pointer. If it disagrees with `00-INDEX.md`, **00 wins**.

Do **not** treat root `OUTPUT_GAP_ANALYSIS.md`, `IMAGE_TO_LOOP_WORKFLOW.md`, or `docs/REPRO_LOCKS_PLAYBOOK.md` as truth — stubs.
