# Repro (minimal)

Closed-product reproduction lives in:

- **`sources/approved/`** — original PNGs  
- **`recipes/locks/`** — final scene + gate JSON  

See **`recipes/locks/README.md`**.

No MP4s, no layer packs in git. Scaffold rebuilds fields after pull.
