// Techno Master — combines all patterns with filter/delay automation baked in

Pdef(\technoMaster,
	Ppar([
		Pdef(\technoKick),
		Pdef(\technoHat),
		Pdef(\technoClap),
	])
);

"Techno master pattern loaded.".postln;
